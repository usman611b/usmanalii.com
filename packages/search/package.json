{
  "name": "@usmanalii/search",
  "version": "0.0.1",
  "private": true,
  "description": "Public search index and authenticated D1 search boundary.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "vitest run",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@usmanalii/contracts": "workspace:*",
    "@usmanalii/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
