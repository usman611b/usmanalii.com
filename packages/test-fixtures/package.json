{
  "name": "@usmanalii/test-fixtures",
  "version": "0.0.1",
  "private": true,
  "description": "Synthetic evidence/project/content fixtures. MUST NOT contain invented facts about Usman Ali.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "vitest run",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@usmanalii/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  }
}
