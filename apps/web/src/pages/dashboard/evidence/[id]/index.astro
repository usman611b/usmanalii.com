---
import DashboardLayout from '../../../../layouts/DashboardLayout.astro';
import { EvidenceWorkspace } from '../../../../components/dashboard/EvidenceWorkspace';
export function getStaticPaths() {
  return [{ params: { id: 'record' } }];
}
---

<DashboardLayout title="Evidence — Command Center" activeSection="evidence"
  ><EvidenceWorkspace client:load /></DashboardLayout
>
