---
import DashboardLayout from '../../../../layouts/DashboardLayout.astro';
import { ProjectWorkspace } from '../../../../components/dashboard/ProjectWorkspace';
export function getStaticPaths() {
  return [{ params: { id: 'record' } }];
}
---

<DashboardLayout title="Project Workspace — Command Center" activeSection="projects"
  ><ProjectWorkspace client:load /></DashboardLayout
>
