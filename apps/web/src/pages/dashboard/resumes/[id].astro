---
import DashboardLayout from '../../../layouts/DashboardLayout.astro';
import { ResumeEditor } from '../../../components/dashboard/ResumeEditor';
export function getStaticPaths() {
  return [{ params: { id: 'record' } }];
}
---

<DashboardLayout
  title="Résumé Composer — Dashboard"
  description="Edit, publish, and restore a canonical résumé projection."
  currentPath="/dashboard/resumes"
>
  <ResumeEditor client:load />
</DashboardLayout>
