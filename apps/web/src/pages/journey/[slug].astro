---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { JournalArticle } from '../../components/JournalArticle';
export function getStaticPaths() {
  return [{ params: { slug: 'record' } }];
}
---

<BaseLayout title="Engineering Journal Entry — USMAN ALI" currentPath="/journey">
  <div class="public-page-shell journal-page-shell"><JournalArticle client:load /></div>
</BaseLayout>
