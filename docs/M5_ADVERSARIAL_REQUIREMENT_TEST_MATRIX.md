# M5 Adversarial Requirement-to-Test Matrix

| Requirement                                                | Exact automated test                                                                                                                                                                                                                                                    |
| ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| JSON authoritative; Markdown derived and non-authoritative | `M5 canonical project revision storage > JSON blocks are authoritative and Markdown is a derived export`                                                                                                                                                                |
| Supported-block round trip                                 | `M5 canonical project revision storage > round-trip conversion preserves supported semantic blocks`                                                                                                                                                                     |
| Malformed, unsupported version, unsafe block               | `Milestone M5 — Project Rules & URL Policy Tests > 14. ADR-005 canonical JSON rejects malformed, unsupported versions and unsupported blocks`                                                                                                                           |
| Rollback appends a revision                                | `D1 Database Repositories — Integration Tests > 3. Rollback creates a NEW revision while original revisions remain 100% immutable`                                                                                                                                      |
| Revision history immutable                                 | `M5 migration adds canonical JSON, immutable revisions, and denies sensitive originals` plus fresh/upgrade D1 execution of migration 015                                                                                                                                |
| Autosave concurrency                                       | `Content Autosave & Local Recovery Manager (Requirement 7) > 4. Optimistic concurrency conflict & silent overwrite prevention` and project repository optimistic `version_no` predicate                                                                                 |
| Child privacy and mode consistency                         | `Milestone M5 — Project Rules & URL Policy Tests > 17. every public child category uses identical privacy eligibility in all modes`                                                                                                                                     |
| Contribution approval/privacy                              | `Milestone M5 — Project Rules & URL Policy Tests > 13. Gate 4: getPublicProjectProjection filters ineligible children and ensures mode consistency`                                                                                                                     |
| URL normalization/adversarial corpus                       | `Milestone M5 — Project Rules & URL Policy Tests > 15. public-link policy rejects ...` and `16. public-link policy normalizes trailing dots, Unicode and punycode without fetching`                                                                                     |
| Search removal and bounded JSON                            | `Milestone M5 — Search Projection Engine Tests > 4. bounded JSON extraction fails closed and removes content after publication changes`                                                                                                                                 |
| Sitemap/direct-route/robots removal                        | `Requirement 4: Publish / Unpublish Propagation Tests > 6. Unpublishing propagation: transitioning published item to unlisted immediately purges it from feed, sitemap, index, and direct route` and search test 5                                                      |
| Safe JSON-LD                                               | `Adversarial XSS & Security Boundary Tests (Gate 4 & 5) > 2. escapeJsonLd safely escapes </script> and <!-- injection sequences` and `Milestone M5 — Search Projection Engine Tests > 5. search, sitemap, canonical, robots, Open Graph and JSON-LD disappear together` |
| Engineering child owner scoping/IDOR                       | `D1 Database Repositories — Integration Tests > 15. M5 IDOR: every engineering child read is scoped by owner and project` covers contribution, experiment, ADR, debugging lesson, deployment, version, and relationship reads                                           |
| Link forgery/cycles                                        | `Milestone M5 — Project Rules & URL Policy Tests > 8. validateProjectRelationship enforces non-empty IDs, self-link prevention, and relevance scale 1-5` and `12. Gate 3: validateProjectCycle and validateVersionCycle detect cycles`                                  |
| Partial transaction rollback                               | `D1 Database Repositories — Integration Tests > 4. Failed D1 batch creates NO partial state (0 side effects)`                                                                                                                                                           |
| Sensitive-original non-storage                             | `M5 migration adds canonical JSON, immutable revisions, and denies sensitive originals`; domain/repository/search/export types expose no sensitive-original field                                                                                                       |

The matrix names registered tests rather than broad suite labels. Final counts and command results belong in the closure walkthrough after execution.
